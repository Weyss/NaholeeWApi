{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('css/search.css') }}">
{% endblock %}

{% block body %}
<main>
    <h2>Résultats</h2>
    <ul>
        {% for result in results %}
        <li><a href="{{ path('detail_'~ result.media, {id:result.id_tmdb}) }}">{{ result.title }}</a></li>
        {% endfor %}
    </ul>
</main>
{% endblock %}
